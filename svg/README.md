# svg/

Composed GEST3015 orbital-atlas sheets land here (`python tools/compose_orbital_atlas.py`).

`matlab_export/` holds the per-panel SVGs written by MATLAB `make_orbital_atlas_svgs` (overwrite Octave previews before submitting figures).
